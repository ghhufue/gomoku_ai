# 五子棋 AI (Gomoku) 项目需求与设计文档

## 一、 项目目标 (Project Objectives)
* **核心诉求：** 开发一个具备基础对弈逻辑、攻防体系完整的五子棋 AI，而非极致算力的“棋神”。
* **性能指标：** 要求模型能在有限算力下**快速收敛**，训练周期短（控制在几天内）。

## 二、 算法选型与训练框架 (Algorithm & Framework)
* **核心算法：** **PPO (Proximal Policy Optimization)**。利用其 On-policy 的稳定性，结合密集奖励机制，实现快速梯度更新。
* **训练机制：** 坚决摒弃“单步更新”。采用 **“批量收集经验 (Rollout) -> 集中更新网络 (Update)”** 的交替循环。
* **参数建议：**
  * 开启并行环境 (`n_envs`): 16 ~ 32 个。
  * 单环境收集步数 (`n_steps`): 128 或 256 步（总 Batch Size = `n_envs * n_steps`）。
  * 小批量大小 (`batch_size`): 256 或 512。
  * 更新轮数 (`n_epochs`): 4 ~ 10 轮。

## 三、 奖惩机制设计 (Reward Shaping)
为解决强化学习在棋类游戏中的“稀疏奖励”问题，引入阶梯式、符号化的密集奖励机制，引导模型快速建立攻防概念，同时利用分级防止“奖励黑客 (Reward Hacking)”行为。

| 级别 | 事件类型 | 触发条件 | 奖励分值 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| **T0 (绝对终结)** | 胜利 | 形成五连 | **+1000** | 结束 Episode |
| | 失败 | 被对方五连 | **-1000** | 结束 Episode |
| | 违规 | 下在已有棋子处 | **-1000** | 强制结束 Episode |
| **T1 (生死攸关)** | 绝杀 | 形成活四 / 冲四 | **+100** | 必胜手 |
| | 救命 | 堵住对方活四 / 冲四 | **+100** | 强行续命 |
| **T2 (优势积累)** | 进攻 | 形成双三 / 活三 | **+20** | 建立优势 |
| | 防守 | 堵住对方活三 | **+20** | 破坏对方优势 |
| | 试探 | 形成活二 | **+5** | 鼓励有效连子 |
| **Base (时间惩罚)** | 存活开销 | 每走一步 | **-1** | 鼓励速战速决 |

## 四、 神经网络架构 (Model Architecture)
采用 **轻量级残差卷积神经网络 (Lightweight ResNet)**，结合 PPO 的 Actor-Critic 双头输出架构。该结构空间平移不变性好、不易梯度消失，且对底层硬件的算子兼容性极佳。

1. **输入层 (Input)：** $15 \times 15 \times 3$ 的多通道 One-hot 编码矩阵。
   * 通道 0：己方棋子位置。
   * 通道 1：敌方棋子位置。
   * 通道 2：合法落子点（空位）。
2. **共享主干 (Shared Body)：** 负责提取“双三”、“冲四”等二维空间特征。
   * 初始卷积层（将 3 通道映射为 64 通道，$3 \times 3$ 卷积）。
   * **3 ~ 5 个残差块 (ResBlock)**（结构：`Conv2D -> BatchNorm -> ReLU -> Conv2D -> BatchNorm -> Add -> ReLU`）。
3. **策略头 (Actor Head)：** 决定下哪里。
   * 输出维度：225。
   * 必须加入 **动作掩码 (Action Masking)**：物理屏蔽已落子位置的概率，再进行 Softmax 操作。
4. **价值头 (Critic Head)：** 评估局势。
   * 经过全连接层降维，最终输出 1 个神经元。
   * 使用 `Tanh` 激活，输出 $[-1, 1]$ 之间的局势评分。

## 五、 底层计算与 C++ 性能优化 (C++ Core Optimization)
强化学习的 Env 环境交互（特别是密集奖励的计算）会成为 CPU 吞吐量瓶颈，必须将环境状态机和奖励计算沉淀到 C++ 底层。

1. **局部增量更新 (Incremental Evaluation)：**
   * 落子后，绝不全盘扫描。仅提取经过落子点 $(x, y)$ 的 4 条线（横、竖、左斜、右斜）的状态进行奖励差值计算（新奖励减去旧奖励）。将复杂度降至 $O(1)$ 级别。
2. **位棋盘与查表法 (Bitboards & LUT)：**
   * 将一条线上 15 个位置的状态（黑/白/空）压缩进整数（位运算）。
   * 预先在内存中生成静态查表（Lookup Table）。运行时截取线段特征直接作为数组下标访问奖励值，彻底消灭耗时的 `if-else` 分支预测。
3. **零拷贝通信 (Zero-Copy Interface)：**
   * 使用 **Pybind11** 封装 C++ 逻辑供 Python 调用。
   * 传递棋盘状态时，直接传递连续内存块的底层指针（如 NumPy array 的数据指针），避免 Python 对象序列化开销，确保数据缓存友好。

## 六、 训练阶段与可视化 (Training Phases & Visualization)

### 1. 阶段式训练策略
* **阶段一（破冰期）：** AI vs 规则机器人（Rule-based Bot）。让网络快速学会简单的攻防（堵三、连四），吃透密集奖励规则。
* **阶段二（进阶期）：** 胜率达标后切入 **自我博弈 (Self-Play)**。维护一个历史模型池，当前最新模型随机抽取历史版本的自己进行对弈，防止策略遗忘。

### 2. 过程监控与可视化
* 推荐使用 **TensorBoard** 或 **Weights & Biases (W&B)**。
* **核心监控指标：**
  * `ep_rew_mean`：回合平均总奖励（应呈阶梯上升）。
  * `ep_len_mean`：回合平均步数（初期变长，学会赢棋后应逐渐缩短）。
  * `value_loss`：价值网络的预测误差（应逐渐下降并收敛）。
  * `policy_entropy`：策略熵，反映探索程度（随训练进行缓慢下降，若骤降至 0 则说明模型陷入局部最优）。